<nav id="navbar" class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid ps-5 pe-5">
    <a class="navbar-brand text-success p-4" role="button" href="/"
      ><h4><i class="bi bi-behance"></i> <span>E-Commerce</span></h4></a
    >
    <!-- <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button> -->
    <!-- <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 p-1">
        <li class="nav-item">
          <a class="nav-link" href="/order"
            >Order</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/product"
            >Product</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/profile"
            >Profile</a
          >
        </li>
      </ul> -->
      <div class="d-flex">
        <% if (locals.admin) { %>
        <div class="dropdown">
          <a
            class="text-success dropdown-toggle"
            id="navbarDropdown"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
            style="text-decoration: none"
          >
            <i class="bi bi-person-circle"></i> <%=
            locals.admin.name%>
          </a>
          <ul
            class="dropdown-menu dropdown-menu-start dropdown-menu-lg-end"
            aria-labelledby="navbarDropdown"
          >
            <li>
                <form
                method="post"
                action="/admin?_method=put&updateToko=1"
                class="dropdown-item"
              >
                <input
                  type="hidden"
                  name="isOpen"
                  value="<%= locals.admin.isOpen %>"
                />
                <button
                  type="submit"
                  class="btn text-secondary"
                  style="border: 0; padding: 0"
                >
                  <% if (locals.admin.isOpen) { %> Tutup Toko <% } else { %> Buka Toko <% } %>
                </button>
              </form>
            </li>
            <li><hr class="dropdown-divider" /></li>
            <li>
              <a
                class="dropdown-item text-success text-decoration-none"
                type="button"
                data-bs-toggle="modal"
                data-bs-target="#updateAdmin"
                >Edit Toko</a
              >
            </li>
            <li>
              <a
                class="dropdown-item text-danger text-decoration-none"
                href="/admin/signout"
                >Logout</a
              >
            </li>
          </ul>
        </div>
        <% } %>
      </div>
      <!-- <% if (locals.admin) { %>
        <div class="d-flex">
          <a class="text-danger" href="/admin/signout" style="text-decoration: none">
            <h6>
              <i class="bi bi-box-arrow-in-right"></i> Keluar
            </h6>
          </a>
        </div>
      <% } %> -->
      </div>
    </div>
  </div>
</nav>
